import { useTexture, useGLTF, useEnvironment } from "@react-three/drei"

export const useTextureImpl = useTexture
export const useTexturesImpl = useTexture
export const useGLTFImpl = useGLTF
export const preloadGLTFImpl = useGLTF.preload
export const useEnvImpl = useEnvironment

